# Goal

Traduis en arabe les champs vides `_ar` du jeu de données ci-après. Pour chaque champ `_ar` vide, traduis le champ `_fr` correspondant en appelant la fonction `UpdateRecord`. Tu dois traiter **tous** les champs vides de **toutes** les entrées.

**Important :** Emet tous tes appels `UpdateRecord` en parallel tool calls dans un seul bloc de réponse. N'appelle pas les fonctions une par une. Si 5 records ont chacun 3 champs vides, emets 15 appels UpdateRecord en parallèle.

# Success

- Tous les champs `_ar` vides sont traduits via des appels `UpdateRecord`
- Aucun texte libre dans ta réponse — uniquement des appels de fonction
- Cohérence exacte des noms de familles entre entrées (mapping ci-dessous)
- Registre déclaratif/nominal pour les descriptions (pas d'impératif)
- Arabe standard moderne (فصحى)

# Mapping canonique des familles

- `Argument valable` (racine) → `حجة معتبرة`
- `Argument pertinent` → `حجة ملائمة`
- `Présentation intègre` → `عرض نزيه`
- `Sens quantitatif` → `استدلال كمي`
- `Inférence maîtrisée` → `إتقان الاستنتاج`
- `Justesse lexicale` → `دقة لفظية`
- `Honnêteté intellectuelle` → `أمانة فكرية`
- `Échange enrichissant` → `تبادل مُثرٍ`

Noms FR **historiques**, retirés de la taxonomie : s'ils apparaissent dans une source ancienne, traduire vers le libellé **courant** ci-dessus — jamais vers l'ancienne cible.
- `Arguments vertueux` → `حجة معتبرة`
- `Exactitude mathématique` → `استدلال كمي`
- `Rigueur mathématique` → `استدلال كمي`
- `Raisonnement valide` → `إتقان الاستنتاج`
- `Langage rigoureux` → `دقة لفظية`
- `Langage exact` → `دقة لفظية`
- `Débat respectueux` → `تبادل مُثرٍ`

Pour les sous-familles non listées, traduis la première occurrence puis réutilise mot pour mot.

# Constraints

- "Argumentum" reste "Argumentum"
- Liens Wikipedia AR : uniquement si certain, sinon laisser vide
- Champs déjà remplis : tu peux les améliorer si tu identifies une erreur, mais priorité aux champs vides

Es-tu prêt à recevoir les données ?
