Convert the input text into a belief set using {{$logicType}} logic, as in the following example, which is in propositional logic. Beliefset will then be parsed by Tweety reasoner engine for analysis. Note that propositional logic does not have implication connector. Instead, it uses the following equivalence: a -> b is equivalent to !a || b

[Input]
Si Alice, Bob ou Charlie sont présents, alors la réunion peut avoir lieu. Si Alice n'est pas là, alors Bob doit être là. Si Bob n'est pas là, alors Charlie doit être là. Enfin, si Charlie n'est pas là, alors Alice, Bob, Charlie et Daniel ne doivent pas être là
+++++
a || b || c
!a || b
!b || c
!c || (!a && !b && !c && !d)
[Input]
[[{{$input}}]]
+++++
