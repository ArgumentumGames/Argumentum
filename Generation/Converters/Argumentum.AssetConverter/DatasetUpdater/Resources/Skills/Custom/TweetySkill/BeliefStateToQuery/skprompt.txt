Given the above text and corresponding translation into a belief set, propose a query to be tested with a reasoning engine in the form of a string formula to be parsed using {{$logicType}} logic, as in the following example

[Input]
===TEXT TO CONVERT
Si Alice, Bob ou Charlie sont présents, alors la réunion peut avoir lieu. Si Alice n'est pas là, alors Bob doit être là. Si Bob n'est pas là, alors Charlie doit être là. Enfin, si Charlie n'est pas là, alors Alice, Bob, Charlie et Daniel ne doivent pas être là.
===END TEXT
===BELIEF SET TO CONVERT
a || b || c
!a || b
!b || c
!c || (!a && !b && !c && !d)
===END BELIEF SET
+++++
!a && !b
[Input]
===TEXT TO CONVERT
{{$input}}
===END TEXT
===BELIEF SET TO CONVERT
{{$belief_set}}
===END BELIEF SET
+++++
