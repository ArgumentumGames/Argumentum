# #458 pool v17 grain 9 — `example_en_bis` : **rien ne se rend, par construction** (mesure, ⛔ 0 écriture)

**Date :** 2026-09-26 · **Lane :** po-2024 (worker) · **Dispatch :** pool v17 grain 9 ([#458 c.5843387663](https://github.com/ArgumentumGames/Argumentum/issues/458#issuecomment-5843387663)) · **Base :** `880b107d` (origin/master)

## 0. Verdict

**La colonne ne rend RIEN — ni bloc vide, ni décalage de mise en page, sur aucun CardSet, aucune langue, Debug comme Release.** La prémisse du dispatch (« référencée par 4 gabarits ») appelle une précision d'instrument : `example_en_bis` est référencée par **6 gabarits**, mais **jamais dans leur clé `mustache`** — uniquement dans l'en-tête de leur clé `csv` **embarquée**, ce snapshot figé que le pipeline **écrase à l'exécution** ([`HarvestManager.cs:363`](../../Generation/Converters/Argumentum.AssetConverter/WebBasedGenerator/Cardpen/HarvestManager.cs)). Aucun jeton `{{example_en_bis}}` n'existe nulle part : rien ne peut créer un nœud DOM pour cette colonne. ⛔ 0 écriture.

## 1. La chaîne runtime, close aux trois niveaux

| Niveau | Fait | Preuve |
|---|---|---|
| **Gabarits** | 6 fichiers portent la chaîne, tous dans la clé `csv` embarquée (4 Fallacies Face + 2 Memo) ; **0 jeton dans `mustache`** | balayage `Cards/**/*.json` hors Archive : 6 hits `csv`, 0 hit `mustache` |
| **Injection** | le csv embarqué est **remplacé** par le CSV vivant avant rendu | `cardSetDocumentWrapper.CardSetDocument.csv = csvContent` — Golden Master restauré (#190) |
| **Données** | CSV vivant : colonne présente, **0 valeur non vide / 1408 lignes** | recompté ce jour |

Un jeton absent ne peut rien rendre — la question « le gabarit rend-il un bloc vide qui décale la mise en page ? » n'a pas de cas : il n'existe aucun site de rendu. Un rendu Debug d'une carte photographierait une absence ; la chaîne ci-dessus la prouve par construction aux trois niveaux indépendants (gabarit, injection, données).

## 2. Le référentiel de la colonne est un instrument mort — famille #1390

La clé `csv` embarquée est le mécanisme déjà nommé par le mémoire de lane (« gabarit Rules : csv embarqué = instrument mort », #1390) : un aperçu figé d'un **ancien état du CSV** (169 lignes, contre 1408 vivantes), inclus dans le gabarit pour la prévisualisation CardPen et jamais consommé par le pipeline. L'en-tête de ce snapshot porte `example_en_bis` entre `example_en` et `link_en` — et même **ce snapshot figé contient 0 valeur** dans la colonne : elle est née vide et ne l'a jamais quittée. La colonne est donc **inerte de bout en bout** : vide dans le vivant, vide dans le figé, sans consommateur.

## 3. Conséquences

- **Aucune action de corpus** ne se justifie : remplir `example_en_bis` ne changerait aucun rendu (aucun jeton) — même pas les mindmaps ni l'OWL, qui consomment d'autres champs.
- La supprimer du CSV vivant serait sans effet rendu également — mais c'est une écriture de schéma hors délégation, ⛔ non proposée.
- La remarque du dossier grain 6 (#1499) se précise : *« une colonne référencée par un gabarit n'est pas une colonne peuplée »* — ici la forme la plus faible encore : **une colonne référencée par le csv embarqué d'un gabarit n'est même pas référencée par le gabarit**. Le décompte « 4 gabarits » du dispatch comptait la clé morte, pas la clé vivante.

## 4. Instrument (rejouable)

```bash
python - <<'EOF'
import json
from pathlib import Path
for f in Path("Cards").rglob("*.json"):
    if "Archive" in f.parts: continue
    try: d = json.load(open(f, encoding="utf-8-sig"))
    except Exception: continue
    if isinstance(d, dict):
        mus, csvk = d.get("mustache",""), d.get("csv","")
        if "example_en_bis" in mus: print(f, "MUSTACHE")
        elif "example_en_bis" in csvk: print(f, "csv embarqué")
EOF
# + CSV vivant : python -c "import csv; ..." — colonne example_en_bis, 0/1408 non vides
```

Mesuré sur `880b107d` : 6 hits `csv embarqué` (4 Fallacies Face{,_2,_3,_Web} + 2 Memo {Face,Back}), 0 hit `mustache`, 0/1408 valeurs vivantes, 0/169 dans le snapshot figé. ⛔ Aucune écriture, aucun appel payant, aucune régénération.
