# #1499 G9-A-W — écriture des cellules désalignées actionnables (492 × 3)

**Date :** 2026-09-26 · **Lane :** po-2024 (worker) · **Dispatch :** pool v14 grain 4 ([#458 c.5838808923](https://github.com/ArgumentumGames/Argumentum/issues/458#issuecomment-5838808923)) · **Mesure source :** G9-A (PR #1564)
**Base :** `906284ab` · **⛔ Aucun appel payant. Aucune régénération.**

## 0. Résultat

| | |
|---|---|
| Cellules écrites | **3** — 492 `text_{es,fa,zh}` |
| Rangées changées | **1** (`git diff --numstat` : 1/1) |
| Octets | `4172087` → `4172092` (**+5**) |
| Champs par ligne | **104 sur les 1408 lignes**, avant comme après |
| Suite locale | **1189 / 0 / 6 (1195)** — dont la garde 1361 **verte** |

| cellule | avant | après |
|---|---|---|
| `text_es` | Auto-victimización | **Hacerse la víctima** |
| `text_fa` | خود-قربانی‌گری | **قربانی جلوه دادن** |
| `text_zh` | 自我受害 | **扮演受害者** |

La carte rejoint la famille « jouer la victime » déjà portée par fr (*Jouer la victime*, G1-W #1522) · en (*Playing the Victim*) · ru (*Игра в жертву*) · pt (*Jogar a Vítima*) · ar (لعب دور الضحية). L'ancien titre fr (« Auto-victimisation ») survivait dans ces 3 langues seules.

## 1. Le premier jet à 4 cellules — et la garde qui l'a arrêté

⚠️ La première exécution portait **4** cellules : les 3 ci-dessus **plus 1361 `example_ar`** (vouvoiement). La suite locale a **rougi** :

```
Pk1361_Example_Matches_The_Polite_Register_Decision [FAIL]
"PK 1361 must carry the Q-12 3a register conversion (fr/ru/pt/es/zh/fa;
 ar and en deliberately unchanged, pinned); drifted cells: 1361.example_ar"
```

Le périmètre exécuté de [#1546](https://github.com/ArgumentumGames/Argumentum/pull/1546) dit : « **ar inchangé (l'arabe standard ne distingue pas tu/vous)** » — arbitrage ai-01 ([c.5825475175](https://github.com/ArgumentumGames/Argumentum/issues/458#issuecomment-5825475175)). **Le rouge était le signal, pas l'obstacle** : l'écriture a été révertée depuis le backup (`cp`, jamais `git checkout --`), jamais poussée, et la mesure G9-A porte son erratum (1361 → VOULU, PR #1564 tête `d18b2d11`).

⭐ **Avant de classer DÉSALIGNÉ une cellule, chercher la garde qui l'épingle** (`grep` du PK dans les tests) : deux faux positifs évités ce cycle par ce seul geste (1361 par la garde ; 796:fa par la cohérence décision/PR/garde).

## 2. Hors périmètre, nommé

- **796 `example_fa`** — glose non restaurée. L'exclusion est portée par **trois artefacts cohérents** : la décision Q-16 (c) (« PK 796 ×6 »), la PR #1549 qui ne nomme pas fa, et la garde d'usine qui épingle l'état coupé. La réouverture est un **arbitrage ai-01** — demande portée sur le dashboard.
- **1361 `example_ar`** — voir §1 : VOULU (décision #1546), épinglé.

## 3. Gardes

| Garde | Ce qu'elle établit | Résultat |
|---|---|---|
| (1) périmètre | diff de valeurs == **exactement** les 3 cellules 492 | ✔ |
| (2) épinglage | les 3 anciennes == attendues ; les 5 langues alignées de 492 == leurs valeurs ; **1361 non touché** | ✔ |
| (3) structure | 1408 lignes, BOM, terminaisons, **104 champs/ligne** | ✔ |
| (4) re-parse | `csv` stdlib : 3 cellules == worklist | ✔ |
| (5) post-état | typographie #994 sur les 3 valeurs (aucune ne porte d'apostrophe/guillemet) | ✔ |
| (6) mutation | 3 mutations rouges (ancienne valeur · apostrophe droite · espace de tête), **témoin vert** | ✔ |

⚠️ Le vérificateur indépendant `994-verifie-BC-independant.py` vit sur la branche #1562 (non mergée) : le contrôle croisé typographique complet reprendra au merge ; la garde (5) couvre les 3 valeurs écrites.

## 4. Limites

- **es/fa/zh sans relecture native** (délégation du 09/09 : choix linguistique ordinaire, décidé puis rapporté — veto ouvert). es « Hacerse la víctima » est la forme idiomatique ; fa « قربانی جلوه دادن » la construction usuelle « poser en victime » ; zh « 扮演受害者 » la forme standard.
- La fratrie 2.3.2.3 est majoritairement nominale (es « Chantaje emocional »…) : le titre 492 reste verbal dans les 8 langues — cohérent avec en/ru/pt/ar déjà verbaux.

## 5. Exécutable

```bash
python tools/1499-g9aw-write-byte-exact.py             # --check : plan + gardes, 0 écriture
python tools/1499-g9aw-write-byte-exact.py --apply     # écrit + backup .BEFORE-G9AW
python tools/1499-g9aw-write-byte-exact.py --mutation-test
```
