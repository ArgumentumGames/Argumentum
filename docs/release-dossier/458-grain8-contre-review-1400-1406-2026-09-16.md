# Pool #458 grain ⑧ (po-2023) — Contre-review croisée des PR po-2024 #1400-#1406

**Auteur** : po-2023 (worker lane) · **Date** : 2026-09-16 · **Base de lecture** : `origin/master` `fe567f5a`
(claims re-jouées sur **leur base déclarée `6fbde739`** quand le drift post-merge rendait la re-lecture naïve trompeuse).
**Nature** : lecture seule — HTTP HEAD public, `git grep`, diff CSV par python, lecture de code. ⛔ 0 write, 0 correction
posée (le grain dit : **consigner sans corriger**). HARD-read fait : bodies intégraux + commentaires (0) + reviews (0) +
diffs vérifiés via les artefacts mergés.

---

## §0 — Verdict en une ligne

**7/7 PR conformes sur les claims falsifiables re-jouées** — dont 4 recomptes numériques **exacts à l'unité près**
(44, 3 320, 502, 11×3) — avec **2 réserves consignées sur #1403** (aucune n'invalide la conclusion opérationnelle).

## §1 — Le tableau des re-jeux

| PR | claim re-jouée (instrument) | verdict |
|---|---|---|
| **#1400** | Content-Type servi : 2 HEAD sur `…/docs/ontology/{argumentum,argumentum_virtues}.owl` + témoins vivant (racine 200) / mort (404) | **REPRODUIT** — `200` ×2, `Content-Type: application/rdf+xml` ×2, Content-Length 5 985 727 / 1 103 945 = blobs committés. ⚠️ mon premier re-jeu rendait 404 : mauvais chemin (l'ontologie vit sous `/docs/ontology/`, `static.yml:53`), pas un état du site. |
| **#1401** | Sweep « 0 porteur » de 18/20/24 comme état courant du Rules : re-passe `Rules.{0,80}\b(18\|24)\b` sur tous les `.md` + count-test | **REPRODUIT** — chaque hit est historique/daté/objet-autre (tick 24 ≠ compte, « cards 7-24 » = plage CSS, plan mars-2026) ou **couvert par erratum in-file** : `validation-guide.fr.md:69` porte bien « Rules (Tarot) \| 24 × 8 \| 192 ✅ » mais l'erratum comptes du **12/09** vit un paragraphe plus bas (« mesure du run 2026-06-03, conservée comme historique… Rules = 15 »). `CardSetExpectedCardCountContractTests:45` épingle 15. |
| **#1402** | ES romance 11 cellules : diff CSV par python (clé `path`), comptes `subcategory_{es,pt}` + EN | **REPRODUIT** — ES 11 · PT 11 · EN 11 (insensible à la casse). |
| **#1403** | « 44× `Model = "gpt-5.5"` + 4× gpt-5.4 » dans `DatasetUpdaterRootConfig.cs` sur `6fbde739` | **REPRODUIT** — `grep -o 'Model = "gpt-5\.5"'` = **44 exact** (et 4× gpt-5.4). ⚠️ Mon premier compte (70) était la **mauvaise unité** (toute occurrence du string, pas l'affectation `Model =`) — l'instrument publié par le dossier tranche. |
| **#1404** | 3 Dependabot DNN lock-only, 0 npm en CI | **REPRODUIT** — #1328/#1323/#1322 : `package-lock.json` seul ×3 ; `grep -l npm .github/workflows/*.yml` = **0/5**. |
| **#1405** | Faits de code : `TargetPath` (écritures ~l.434), `TakeChunkNb=-1` défaut (l.72), `CompareMode` saute la traduction (l.163), `SelectEmptyTargets` commenté (l.176+) | **REPRODUIT** — les 4 vérifiés dans `DatasetUpdaterConfig.cs` : le chemin complet (chargement chunks + traduction) est **dans** `if (!CompareMode)`. |
| **#1406** | Comptes `link_*` immobiles : population `link_fr` non vide, gap par langue, parts wiki 88 %/74 % | **REPRODUIT intégralement sous l'instrument publié** — Fallacies **637**/1408, gap **3 320** exact ; Virtues **216**/223, vides **487** + **15** cellules « FR-URL copiées » (4+6+2+3, unité nommée dans le tableau du dossier) = **502** ; total **3 822** ; wiki par **hôte** : tout-wikipedia 551 + tout-wiktionary 11 = **562/637 = 88,2 %**, Virtues **161/216 = 74 %** exact. |

## §2 — Les deux réserves consignées sur #1403 (sans correction posée)

1. **« 0× gpt-5.6 — le nom n'existe nulle part dans le dépôt »** : le sweep couvre les racines **nommées**
   (`docs/`, `tools/`, `Generation/`, README, CLAUDE.md) — mais **2 occurrences `gpt-5.6-sol` préexistaient sur la base
   `6fbde739`** dans `.claude/skills/coordinate/SKILL.md` (l.20 et l.164, trackées). « Tout le dépôt » est dit trop large.
   **Incidence nulle sur la conclusion config** (un skill de coordination n'est pas la config compilée) — et ces
   occurrences **corroborent le GO 14/08** que le dossier qualifiait RAPPORTÉ en cherchant sa trace écrite « la plus
   proche » : elle existe dans le dépôt, hors des racines balayées.
2. **« 1 590 lignes / 79 fichiers citent gpt-5.5 »** : non reproduit par `git grep` sur la base (387 occurrences /
   82 fichiers). L'instrument de ce compte n'est pas publié dans le dossier (le chiffre de tête, lui, est exact et son
   instrument nommé). Population probable ≠ dépôt tracké (working tree ?) — non prouvé, consigné comme tel.

## §3 — Faux positifs de la contre-review elle-même (consignés, la leçon sert le prochain sweep)

1. **70 ≠ 44** : mauvaise unité de ma part tant que je n'avais pas lu l'instrument publié — une contre-review doit
   re-jouer **la claim sous son unité déclarée** avant de compter autrement.
2. **404 Content-Type** : mauvais chemin (racine vs `/docs/ontology/`) — l'erratum #1400 citait les URLs via #133 ;
   le workflow `static.yml` est la source du chemin.
3. **24×8 validation-guide** : candidat porteur désamorcé par l'erratum in-file du 12/09 (la table est un état
   historique rafraîchi par note, pas un porteur courant).
4. **487 ≠ 502 / 552 ≠ 562** : deux unités différentes du même compte (FR-URL copiées ; wiki par hôte avec
   wiktionary, une URL archive.org contenant « wikipedia » en chemin ne compte pas).

## §4 — Ce que cette contre-review n'établit pas

- ⛔ Aucune correction posée (grain : consigner sans corriger) ; les 2 réserves #1403 sont à porter par po-2024/ai-01
  si jugées dignes d'un erratum (mon avis : la réserve 1 mérite une ligne, la réserve 2 un instrument publié).
- ⛔ Pas de re-vérification des « ~70 audits datés » du classement #1403 §1 (échantillonné, pas exhaustif — le dossier
  le déclare lui-même).
- ⛔ Les claims non falsifiables depuis mon siège (appréciations lexicales #1402 §2, reco #1404/#1406, budget dérivé
  #1405) ne sont pas contestées ici.

---

*po-2023 (worker lane) · grain ⑧ du pool c.`5666259809` · lecture seule : HTTP HEAD public + git grep + diff CSV python +
lecture code · base de re-jeu = la base déclarée de chaque claim (`6fbde739`) · verdict visuel : ai-01 · décisions : ai-01/jsboige.*
