@inherits ToSic.Sxc.Dnn.RazorComponent
@using ToSic.Razor.Blade;
@{ var parts = CreateInstance("../shared/_Parts.cshtml"); }

<section class='app-landingpage-builder app-lp-testimonials-brands @parts.SectionClasses()'
    @parts.SectionStyle()
    @Edit.TagToolbar(Header, toolbar: "settings&hover=left&autoAddMore=start")
    >
    @parts.BackgroundDiv()
    <div class="container-fluid">
        @RenderPage("../shared/_Block.cshtml")
        <div class="row justify-content-center">   
            @foreach (var customer in AsList(Data)) {    
                <div class="col-6 col-sm-4 col-lg-3 col-xl-2 mb-4">
                    <div class="h-100 text-center align-items-center" @Edit.TagToolbar(customer, toolbar: "%delete&show=true")>
                        <img class="img-fluid" src='@Tags.SafeUrl(customer.Image + "?w=400&amp;scale=both&amp;mode=max&amp;quality=70")' alt="@customer.Name">
                    </div>
                </div>     
            }
        </div>
        @parts.HeaderCta(Header.CTA)
    </div>
</section>

@RenderPage("../shared/_Assets.cshtml", new { parts = "styles,scripts" })
