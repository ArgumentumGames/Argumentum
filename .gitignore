## Ignorer les fichiers et dossiers générés par .NET
# Dossiers de build
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Ww]in-x64/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]uild/
msbuild.log
msbuild.err
msbuild.wrn

# Fichiers de build
*.dll
*.exe
*.pdb
*.cache
*.suo
*.user
*.userosscache
*.sln.docstates
*.userprefs

# Fichiers de Visual Studio
.vs/
*_i.c
*_p.c
*_h.h
*.ilk
*.meta
*.obj
*.iobj
*.pch
*.ipdb
*.pgc
*.pgd
*.rsp
*.sbr
*.tlb
*.tli
*.tlh
*.tmp
*.tmp_proj
*_wpftmp.csproj
*.log
*.vspscc
*.vssscc
.builds
*.pidb
*.svclog
*.scc

# Fichiers de Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Fichiers de ReSharper
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# Fichiers de test
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.TestResults.xml
*.trx
*.coverage
*.coveragexml

# NuGet
*.nupkg
# Les fichiers de packages peuvent être ignorés car NuGet restaure les packages
**/[Pp]ackages/*
# sauf build/, qui est utilisé comme cible MSBuild
!**/[Pp]ackages/build/
# Décommentez si nécessaire mais généralement il est régénéré quand nécessaire
#!**/[Pp]ackages/repositories.config
# Les fichiers project.json de NuGet v3 produisent plus de fichiers ignorables
*.nuget.props
*.nuget.targets

# Fichiers spécifiques au projet Argumentum
Target/
TempProject/
TempProject2/
TestSpectreConsole/
temp/
temp_build/
win-x64.zip
win-x64/
error.txt
*.bat.old
deploy.ps1
update_spectre_console*.ps1

# Fichiers de sauvegarde
*~
*.bak
*.swp
*.tmp

# Fichiers système
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Fichiers de journalisation
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Fichiers d'environnement
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Fichiers de configuration spécifiques à l'utilisateur
*.user
*.userosscache
*.sln.docstates

# Exclusions spécifiques au projet Argumentum (reprises de l'ancien .gitignore)
/DNNPlatform/App_Data/Search
/Cartes/Generation/Converters/Argumentum.AssetConverter/Data
/Cartes/Generation/Converters/Mindmapper/Data

# Bin DNN (exception pour inclure ce dossier)
!/DNNPlatform/bin/

# Fichiers de configuration locaux
/Chatgpt-plugin/azure-function/local.settings.json


# BFG & Mirror Repo Artifacts
Argumentum.git-mirror/
*.bfg-report/

bfg.jar


# Dossiers de sortie générés
**/[Oo]utput/


# Fichiers et répertoires de projet non suivis
Cards/en/
Cards/fr/
Cards/pt/
Cards/ru/
Scripts/
# Temporary folders
.temp/
temp_owl_sharp/


# Fichiers PDF générés à la racine
/*.pdf

# Dépendances Node.js de CardPen
/Generation/CardPen/node_modules/

**/imageflow_hybrid_cache/

Logs/


# Sorties des tests d'intégration
Generation/Converters/Argumentum.AssetConverter.Tests/TestOutput/

# Fichiers de sortie de test
test_output.txt
*_cleaned.csv

# Configuration générée automatiquement par AssetConverter
AssetConverterConfig.json
AssetConverterConfig.*.json
!AssetConverterConfig.cs
# Exception: VisualTests fixture must be committed (referenced by FallacyCardTests)
!Generation/Converters/Argumentum.AssetConverter.VisualTests/TestData/**/AssetConverterConfig.test.json

# Logs d'erreur temporaires
*.log.err

# Screenshots de validation automatique
docs/investigations/screenshots/validation-finale-*/

docs/investigations/pdf-validation/node_modules/

# Claude Code coordination directory
.claude/*
!.claude/skills/
!.claude/skills/**
!.claude/*checkpoint*.md
!.claude/*debrief*.md

# Validation artifacts (temporary PNGs, PDFs, and output from debug sessions)
Validation/*.png
Validation/*.pdf
Validation/comparison_output/
Validation/pdf_inspection/
Validation/Argumentum_ru_Validation/
comparison_output/

# Playwright MCP cache
.playwright-mcp/

# Mindmap SVG export workspace
_mm_export/

# Generated preview directories
_gen_preview/
_ref_preview/

# Claude Code session artifacts in subdirectories (not root .claude/)
Generation/**/.claude/
.keys/

# Local ai-01 cycle scratch directories (specs, drafts, logs, screenshots)
/cycle*/
/MASTER-INDEX.md
/cycle*.jpeg
/cycle*.png
/title_repro_shot.png

# ============================================================
# Phase 1 — #415: Remove trackable build artifacts from repo
# These are all regenerable via pipeline build or re-downloadable.
# ============================================================

# Published build outputs (.NET self-contained zips & executables)
**/Published/

# DNN distributable downloads (regenerable via pipeline)
DNNPlatform/Portals/*/Downloads/*.zip
DNNPlatform/Portals/*/Downloads/*.zip.REMOVED.git-id

# DNN extension packages (re-downloadable installers)
DNNPlatform/App_Data/ExtensionPackages/*.resources

# DNN module installers (re-downloadable ToSic 2sxc install zips — keep PlaceHolder.txt)
DNNPlatform/Install/Module/*.zip

# Large generated SVGs (regenerable via MindMapper pipeline)
# Note: small source SVGs in Cards/ are kept
Generation/Converters/Argumentum.AssetConverter/Mindmapper/*.svg

# #141 AIF full-scale candidate sidecar — regenerable derived data
# (python docs/taxonomy/141-aif-fullscale.py --finalize regenerates from checkpoint,
#  no API calls). 1.3 MB; per 141-aif-fullscale-report.md it is a tmp/regenerable
#  artifact, not source. Referenced by 7 committed docs/scripts that document the
#  regen path. Aligns #415 (reduce .git weight) — keep local, never commit.
docs/taxonomy/141-aif-candidates-fullscale.json

# Python bytecode / cache — never commit (standard hygiene). The repo ships Python
# tooling in tools/, docs/taxonomy/, docs/investigations/scripts/; __pycache__ is
# machine-specific and churns. Catches the untracked docs/taxonomy/__pycache__/ etc.
__pycache__/
*.py[cod]
*$py.class

# Root tmp/ scratch dir — local regenerable working files (AIF #141/#498 datasets,
#  run logs, audit samples). ~1.3 MB; regenerable from committed scripts (e.g.
#  docs/taxonomy/141-aif-fullscale.py, tools/498-*.py). Machine-specific scratch,
#  never source — root-anchored (/tmp/) so a legit nested tmp/ elsewhere is unaffected.
/tmp/
